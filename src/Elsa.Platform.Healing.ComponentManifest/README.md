# Elsa Platform Healing component manifests

This package contains the versioned application component-manifest contract, canonical serializer, validator, and SHA-256 digest calculator used by Elsa Platform Healing.

The manifest contains only revision-bound component identities, safe relative assembly paths, dependency edges, advisory repository metadata, and artifact hashes. It never requires local package-cache paths, credentials, source contents, or environment variables.
