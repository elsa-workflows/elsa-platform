# Data Model: Deployment Template Expansion

## DeploymentTemplateTarget

Fields: `Id`, `DisplayName`, `SupportedCapabilities`, `RequiredInputs`.

## TemplateBundle

Fields: `Target`, `Files`, `Findings`.

## TargetCapability

Fields: `TargetId`, `RuntimeImageSlug`, `InfrastructureProviderId`, `Supported`.
